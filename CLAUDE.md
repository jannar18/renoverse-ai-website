# See AGENTS.md

This project's agent protocol, house rules, and session checklist live in [`AGENTS.md`](AGENTS.md). That file is the canonical source — `CLAUDE.md` exists only as a pointer for Claude Code versions that look for it by name first.
